# AGENTS Entry Point

Primary agent standards are maintained at:

- `docs/standards/AGENTS.md`

Quick rules that always apply:

- Approval-first: no edits without explicit user permission.
- Minimal-change: smallest valid fix that preserves behavior.
- Runtime focus: prioritize `run.py` -> `api.py` + `web/app.py`.
